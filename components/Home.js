import React, { Component } from 'react';
import { Text } from 'react-native';
import { ImageBackground } from 'react-native';
import { Container, Fab, Card, CardItem, Content } from 'native-base';
import Icon from 'react-native-vector-icons/FontAwesome';

// import styles
import { appColor, appComplementColor } from '../styles';

// import backgrond image
import wallpaper from '../backgroundWallpapers/home.jpg';

class Home extends Component {
  render() {
    return (
      <Container>
        <ImageBackground
          source={wallpaper}
          style={{ width: '100%', height: '100%' }}
        >
          <Content>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
            <Card>
              <CardItem bordered>
                <Text>Title of the first post</Text>
              </CardItem>
              <CardItem bordered>
                <Text>
                  When the state object gets persisted, it first gets serialized
                  with JSON.stringify(). If parts of your state object are not
                  mappable to JSON objects, the serialization process may
                  transform these parts of your state in unexpected ways. For
                  example, the javascript Set type does not exist in JSON. When
                  you try to serialize a Set via JSON.stringify(), it gets
                  converted to an empty object. Probably not what you want.
                </Text>
              </CardItem>
            </Card>
          </Content>
          <Fab
            position='bottomRight'
            style={{ backgroundColor: appComplementColor }}
            onPress={() => this.props.navigation.navigate('NewPost')}
          >
            <Icon name='plus' style={{ color: appColor }} />
          </Fab>
        </ImageBackground>
      </Container>
    );
  }
}

export default Home;
